Notes/Todo Items


Global:
1. Create unit tests with Jest or Enzyme. https://reactjs.org/docs/testing.html
2. Make sure everything is mobile responsive and rendering correctly cross browser 
3. Setting up local and prod environments for deployment
4. Setup axios global basepath based on env
5. change download file path based on env
6. Find hosting solution
   -Add as a subdomain to landaccrue.com
   -Add concurrently package to run back and front in 1 cmd with npm
   -Clean up build with Dev dependencies(for build) and dependencies (needed at run time)
7. Mongodb whitelist Land Accrue ip
8. Make sure to Comment out add admin user route - only use internally
